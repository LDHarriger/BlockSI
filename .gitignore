# ESP-IDF build artifacts
Interfaces/ControlSystem/build/
Interfaces/ControlSystem/sdkconfig
Interfaces/ControlSystem/sdkconfig.old
Interfaces/ControlSystem/dependencies.lock

# Keep sdkconfig.defaults (safe template) but not sdkconfig (has credentials)

# Python
__pycache__/
*.pyc
Interfaces/PC/__pycache__/
Interfaces/PC/.streamlit/

# Data files (don't commit recorded data)
Interfaces/PC/*.csv
Interfaces/PC/Data/
Interfaces/Data/
Data/
# TODO: re-add "Interfaces/Data/CSTR/" to the line above once CSTR sequence is
# stable (currently force-tracked for debugging). Run:
#   git rm -r --cached Interfaces/Data/CSTR
#   and restore the Interfaces/Data/ rule to cover CSTR again.

# Archive — legacy/deprecated files kept locally for reference only
Old/

# IDE
.vscode/
*.code-workspace

# OS files
.DS_Store
Thumbs.db

# Logs
*.log